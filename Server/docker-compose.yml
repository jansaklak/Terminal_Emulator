services:
  terminal-server:
    build:
      context: .
    container_name: python_gateway
    restart: always
    environment:
      PROJECT_PATH: ${PROJECT_PATH:-}
      TZ: Europe/Warsaw
    stop_grace_period: 15s
    ports:
      - "51234:51234"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./logs:/var/log/terminal-server
      - .:/app
    networks:
      - lab-net

  admin-panel:
    build: .
    container_name: admin_panel
    command: python3 web_panel/admin_panel.py
    environment:
      TZ: Europe/Warsaw
    ports:
      - "5001:5001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/app
      - ./logs:/var/log/terminal-server
    networks:
      - lab-net

networks:
  lab-net:
    name: lab-net